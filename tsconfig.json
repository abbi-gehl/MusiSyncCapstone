{
  "extends": "@react-native/typescript-config/tsconfig.json",

  "compilerOptions": {
    "types": ["nativewind/types", "jest"], // Add NativeWind types
    "typeRoots": ["node_modules/@types", "./"] // Ensure custom types are picked up

  },

  "include": [
    "**/*.spec.ts",
    "src/**/*", // Include your source files
    "nativewind-env.d.ts" // Ensure the nativewind-env.d.ts file is included
  ]
}
